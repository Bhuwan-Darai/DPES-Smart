import { View } from "react-native";
import StudentAttendance from "@/components/parent/attendance/StudentAttendance";

const StudentAttendanceScreen = () => {
  return (
    <View style={{ flex: 1 }}>
      <StudentAttendance />
    </View>
  );
};

export default StudentAttendanceScreen;
